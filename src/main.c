#include "pl_utils.h"

struct hello {
    int test;
    int test1;
    int test2;
    int test3;
};

int main(void)
{
    void* hell = malloc(sizeof(int) * 4);
    struct hell* = (void *)(sizeof(struct hell));
    PL_LOG(PL_INFO, "%s", "James is gay.");
    return 0;
}
